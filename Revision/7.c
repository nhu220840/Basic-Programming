#include <stdio.h>

int main(){
    int x = 10;

    //printf("%d, %d, %d\n", x < 1, x = 100, x >= 10);

    //printf("%d, %d, %d\n", x < 1, x = 100, x);
    printf("%d, %d, %d\n", x < 1, x = 100, x <= 10);
}