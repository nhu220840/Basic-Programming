#include <stdio.h>

int main(){
    // p as a pointer to int: con tro
    int *p;

    // p as a pointer to a pointer to int: con tro tro toi con tro
    int **p;

    // p as an array of 10 pointers to int: mang con tro
    int *p[10];
    
    // con tro mang
    int a[10];
    int *p = a;
}