#include <stdio.h>

//Goi a la so bai lam trong 1 ngay
//Goi b la so ngay lam het bai tap
//(a + X)(b - Y) = ab 
// => ab - aY + bX - XY = ab
// => aY - bX = -XY

//(a - Z)(b + 1) = ab
// => ab + a - bZ - Z = ab
int main(){
    int X, Y, Z; scanf("%lld %lld %lld", &X, &Y, &Z);

}