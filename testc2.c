#include <stdio.h>

int main(){
    printf("Enter a month: ");
    int month; scanf("%d", &month);
    if(month <= 0 || month > 12){
        printf("Invalid month!");
    }
    else if(month == 1 || month == 3 || month)
}